#set text(font: "Noto Serif CJK SC", lang: "zh", script: "HanS")

= 他妈的数值分析

+ 几种矩阵分解的区别：
  - Doolittle 分解：形如 $L U$，其中 $L$ 对角线为 1；
  - Crout 分解：形如 $L U$，其中 $U$ 对角线为 1；
  - LDU 分解：形如 $L D U$，其中 $D$ 是对角矩阵，$L$、$U$ 对角线均为 1；

